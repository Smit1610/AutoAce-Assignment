version: '3.8'

services:
  autoace-app:
    build: .
    container_name: autoace-ai-engine
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
